[project]
name = "local-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.0",
    "flask-cors>=5.0.0",
    "python-dotenv>=1.0.1",
    "requests>=2.32.3",
    "pymupdf>=1.23.7",  
    "loguru>=0.7.2",    
    "python-docx>=1.1.0" 
]

[tool.setuptools.packages.find]
where = ["."]
include = ["prompting", "assets"] 
